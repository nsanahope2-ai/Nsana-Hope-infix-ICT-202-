function infixToPrefix(expression):
    reverse expression

    for each character:
        if '(' replace with ')'
        if ')' replace with '('

    postfix = infixToPostfix(reversed expression)

    reverse postfix

    return prefix
